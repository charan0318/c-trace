 index.html
index_html = """<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Chiliz AI Explorer</title>
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;600;800&display=swap" rel="stylesheet" />
  </head>
  <body class="font-sans bg-black text-white">
    <div id="root"></div>
    <script type="module" src="/src/index.js"></script>
  </body>
</html>
"""

# Tailwind config placeholder
tailwind_config = """module.exports = {
  content: ["./src/**/*.{js,jsx}"],
  theme: {
    extend: {
      fontFamily: {
        sans: ['Inter', 'sans-serif'],
      },
    },
  },
  plugins: [],
}
"""

# index.js
index_js = """import React from "react";
import ReactDOM from "react-dom/client";
import App from "./App.jsx";
import "./index.css";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(<App />);
"""

# index.css with Tailwind base
index_css = """@tailwind base;
@tailwind components;
@tailwind utilities;
"""

# App.jsx with all enhancements and no Nebula branding
app_jsx = """import { useState } from "react";
import { motion } from "framer-motion";
import Typical from "react-typical";

export default function App() {
  const [input, setInput] = useState("");

  return (
    <div className="min-h-screen bg-gradient-to-br from-black via-gray-900 to-black text-white font-sans p-4">
      <nav className="flex justify-between items-center px-6 py-4 border-b border-white/10 backdrop-blur-md bg-white/5 rounded-xl shadow-md">
        <h1 className="text-2xl font-bold tracking-wide">🌶️ Chiliz AI Explorer</h1>
        <button className="bg-pink-600 hover:bg-pink-700 text-white px-4 py-2 rounded-full font-semibold shadow-[0_0_12px_#ec4899]">
          Connect Wallet
        </button>
      </nav>

      <main className="flex flex-col items-center mt-24 text-center">
        <motion.h1
          className="text-5xl sm:text-6xl font-bold bg-gradient-to-r from-pink-500 to-purple-500 bg-clip-text text-transparent drop-shadow-lg"
          initial={{ opacity: 0, y: -20 }}
          animate={{ opacity: 1, y: 0 }}
        >
          <Typical
            steps={['Explore Smart Contracts ⚙️', 2000, 'Check Token Balances 💰', 2000, 'Inspect Transactions 🔍', 2000]}
            loop={Infinity}
            wrapper="span"
          />
        </motion.h1>
        <p className="text-lg text-white/70 mt-6 max-w-xl">
          A sleek interface to query and explore Chiliz chain data with natural language.
        </p>

        <div className="mt-12 flex flex-col sm:flex-row gap-4 items-center w-full sm:w-[600px]">
          <input
            className="w-full p-4 rounded-lg bg-white/10 border border-white/10 focus:outline-none focus:ring-2 focus:ring-pink-600 placeholder-white/50 text-white backdrop-blur-md"
            placeholder="Enter contract address or question..."
            value={input}
            onChange={(e) => setInput(e.target.value)}
          />
          <button className="bg-pink-600 hover:bg-pink-700 px-6 py-3 rounded-lg text-white font-semibold shadow-[0_0_12px_#ec4899] transition-all">
            🔍 Search
          </button>
        </div>

        <div className="mt-20 grid grid-cols-1 sm:grid-cols-3 gap-8 w-full max-w-6xl px-4">
          {['Check Balance', 'Get Contract Info', 'Decode Transaction'].map((text, index) => (
            <div key={index} className="bg-white/5 rounded-2xl p-6 backdrop-blur-md shadow-lg border border-white/10 hover:scale-105 transition-all">
              <h3 className="text-xl font-bold text-pink-400 mb-2">{text}</h3>
              <p className="text-white/60">Explore {text.toLowerCase()} easily through AI prompts.</p>
            </div>
          ))}
        </div>
      </main>

      <footer className="mt-24 text-sm text-white/40 text-center">
        &copy; {new Date().getFullYear()} Chiliz AI | Crafted with ❤️
      </footer>
    </div>
  );
}
"""

# package.json with dependencies
package_json = """{
  "name": "chiliz-ui-explorer",
  "version": "1.0.0",
  "scripts": {
    "start": "parcel public/index.html --port 3000"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0",
    "framer-motion": "^10.16.1",
    "react-typical": "^0.1.3"
  },
  "devDependencies": {
    "parcel": "^2.9.3",
    "tailwindcss": "^3.4.1"
  }
}
"""

# Write files
with open(f"{project_dir}/public/index.html", "w") as f:
    f.write(index_html)

with open(f"{project_dir}/src/index.js", "w") as f:
    f.write(index_js)

with open(f"{project_dir}/src/index.css", "w") as f:
    f.write(index_css)

with open(f"{project_dir}/src/App.jsx", "w") as f:
    f.write(app_jsx)

with open(f"{project_dir}/package.json", "w") as f:
    f.write(package_json