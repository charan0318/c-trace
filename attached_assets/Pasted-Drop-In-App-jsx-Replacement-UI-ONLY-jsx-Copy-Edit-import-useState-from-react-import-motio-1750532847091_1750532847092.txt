Drop-In App.jsx Replacement (UI ONLY)
jsx
Copy
Edit
import { useState } from "react";
import { motion } from "framer-motion";

export default function App() {
  const [input, setInput] = useState("");

  return (
    <div className="min-h-screen bg-gradient-to-br from-[#0f0c29] via-[#302b63] to-[#24243e] text-white font-sans p-4">
      <nav className="flex justify-between items-center px-6 py-4 border-b border-white/10 backdrop-blur-md bg-white/5 rounded-xl shadow-md">
        <h1 className="text-2xl font-bold tracking-wide">🌐 Chiliz Nebula</h1>
        <button className="bg-pink-600 hover:bg-pink-700 text-white px-4 py-2 rounded-full font-semibold shadow">
          Connect Wallet
        </button>
      </nav>

      <main className="flex flex-col items-center mt-24 text-center">
        <motion.h1
          className="text-5xl sm:text-6xl font-bold bg-gradient-to-r from-pink-500 to-purple-500 bg-clip-text text-transparent drop-shadow-lg"
          initial={{ opacity: 0, y: -20 }}
          animate={{ opacity: 1, y: 0 }}
        >
          AI-Powered Blockchain Explorer
        </motion.h1>
        <p className="text-lg text-white/70 mt-4 max-w-xl">
          Ask anything about smart contracts, addresses, or transactions on the Chiliz Chain —
          powered by Thirdweb Nebula.
        </p>

        <div className="mt-10 flex flex-col sm:flex-row gap-4 items-center w-full sm:w-[600px]">
          <input
            className="w-full p-3 rounded-lg bg-white/10 border border-white/10 focus:outline-none focus:ring-2 focus:ring-pink-600 placeholder-white/50 text-white"
            placeholder="Enter a contract address or prompt"
            value={input}
            onChange={(e) => setInput(e.target.value)}
          />
          <button className="bg-pink-600 hover:bg-pink-700 px-6 py-3 rounded-lg text-white font-semibold shadow-lg transition-all">
            🔍 Search
          </button>
        </div>
      </main>

      <footer className="mt-24 text-sm text-white/40 text-center">
        &copy; {new Date().getFullYear()} Chiliz AI | Powered by Thirdweb Nebula
      </footer>
    </div>
  );
}